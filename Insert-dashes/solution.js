//ToDo insert - between 2 odd numbers


//find a method that will locate 2 odd numbers together.
    //eval?  parseInt? 
    //separate the the values and .push into an array
    //make interger into a string
    //split the string
    //.push into a array


function insertDash(num) {
    var string = num.toString()
    var separatedString = string.split()
    console.log('separatedString', separatedString);
    for (var i = 0;  i < separatedString.length; i += 1) {   
   
}

insertDash(454793);

//concatenate  the "-" between those 2 odd numbers in this string


//return the concatenated string
